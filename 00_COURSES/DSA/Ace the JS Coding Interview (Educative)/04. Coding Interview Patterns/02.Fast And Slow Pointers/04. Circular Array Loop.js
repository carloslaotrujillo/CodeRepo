/**

create a circular DLL from array
create two pointers (slow)(fast)
slow will move 1 step at the time (ff or back, base on the value of the Node)
fast will move 2 steps at the time (ff or back, base on the value of the Node)

initialize a direction variable
initialize a sequenceLength variable

for (let i = 0; i < array.lenght; i++)
    slow moves 1 step
    fast moves 2 steps
    




1 3 -2 -4 1
^
^

 */

export function circularArrayLoop(nums) {}
